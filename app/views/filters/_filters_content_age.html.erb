<div data-age-target="result">
  <% if @research.feature_ids.include?(Feature.find_by(title: "age").id) %>
    <div class="mb-2">
      <div class="feature-name">
        <h3>age</h3>
      </div>
      <div class="d-flex">
        <div class="px-2"><%= @age_min %> years old to <%= @age_max %> years old</div>
      </div>
    </div>
  <% end %>
</div>
